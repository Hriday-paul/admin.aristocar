import React from 'react';
import ModelTable from './_components/ModelTable';

const page = () => {
    return (
        <div>
            <ModelTable />
        </div>
    );
};

export default page;